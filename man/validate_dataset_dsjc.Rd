% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dsjc.R
\name{validate_dataset_dsjc}
\alias{validate_dataset_dsjc}
\title{Validate a Dataset JSON Compressed (DSJC) file}
\usage{
validate_dataset_dsjc(x)
}
\arguments{
\item{x}{File path of a DSJC file, or a raw vector holding the compressed
bytes}
}
\value{
A data frame of errors, empty when the file is valid
}
\description{
Decompresses the zLib stream and validates the Dataset NDJSON content it
holds, as \code{validate_dataset_ndjson()} does: the metadata object on line 1 is
checked against the Dataset NDJSON v1.1.0 schema, and each subsequent line
must be a JSON array carrying one value per declared column.
}
\examples{
ds_json <- dataset_json(iris, "IG.IRIS", "IRIS", "Iris", columns = iris_items)
validate_dataset_dsjc(write_dataset_dsjc(ds_json))
}
